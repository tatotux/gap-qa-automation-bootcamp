/*//Functions y Promesas 

//Functions
greet1();
//Function Declaration
function greet1() {
    console.log('Hello, world');
}


//Function expressions 
const greet2 = function() {
    console.log('Hello, world');

};


// Arrow functions
const greet4 = (name = 'Anyone') => {
    console.log('Processing...')
    console.log(`Hello ${name}`)
}

const greet3 = (name = 'Anyone') => { console.log('test') }

//Callback functions 
function addName(name, callback) {}
*/

/*Arrays
names.push('Jhon');
console.log(names);
names.push('Jane');
console.log(names);
names.pop();
console.log(names);
//names.pop(2)
console.log(names);
;
const person = {
    name: 'Cristina',
    surname = 'Manjarrez',
    age: 10,
}; 


const names = ['Cristina', 'Juanito', 'Carolina'];
//names.forEach(name) => { console.log(name) }

const rows = names.forEach(name => names.name)
console.log(rows);

let found = names.find((name) => name == 'Cristina');
console.log(found);

const filteredNames = names.filter(name => name.startsWith('C'))
console.log(filteredNames)

console.log(names.every((name) => name.length > 5));
console.log(names.some((name) => name.length > 5));


const numbers = [1, 2, 3, 4, 5];

const poweredNumbers = numbers.map(num => num * num);
console.log(numbers);
console.log(poweredNumbers);

console.log(numbers.reduce((accumulator, currentValue) => accumulator + currentValue));

const [a, b, c] = names;

const person = {
    name: 'Cristina',
    surname = 'Manjarrez',
    age: 10,
};

const { name, age } = person;
*/

//Promises

